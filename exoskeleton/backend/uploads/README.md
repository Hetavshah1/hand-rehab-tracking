This folder stores uploaded exercise video files.

Place exercise video files (.mp4, .webm, etc.) here so they can be served by the backend `/uploads/<filename>` route.

When running locally, you can copy sample videos into this folder to make them available to the Exercise Library UI.
